// const prompt = require('prompt-sync')({ sigint: true });
import * as prompt from 'prompt-sync';
import * as game from './pokenode.js';
// starting the game

console.log(bulbasaur);

game.chooseYourPokemon;

while (game.promptedOpponent.stats.hp > 0) {
  game.takeTurn;
}
